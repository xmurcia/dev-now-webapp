import React, { ReactElement, Props } from 'react';

const PrivateContainer: React.FC<Props<any>> = (): ReactElement => {

  return (
    <div>
      Private Container Component!
    </div>
  );
};

export default PrivateContainer;
